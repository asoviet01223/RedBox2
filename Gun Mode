local player = game.Players.LocalPlayer

local function refillAmmo(tool)
    local varsFolder = tool:FindFirstChild("Vars")
    if not varsFolder then return end
    
    local reserveValue = varsFolder:FindFirstChild("reserve")
    local magazineValue = varsFolder:FindFirstChild("magazine")

    if reserveValue and reserveValue.Value < 6666 then
        reserveValue.Value = 6666
    end
    if magazineValue and magazineValue.Value < 6666 then
        magazineValue.Value = 6666
    end
end

local function continuouslyRefillAmmo()
    while true do
        for _, tool in pairs(player.Backpack:GetChildren()) do
            if tool:IsA("Tool") then
                refillAmmo(tool)
            end
        end
        wait(0.5)
    end
end

spawn(continuouslyRefillAmmo)
